/**
 * This layer contains all the technical concerns that support the other layers.
 * It often includes things like database access (e.g., repository implementations), file I/O, web services,
 * caching and other concerns that are not specific to the domain but are needed to implement the system.
 */
package com.totvs.infrastructure;